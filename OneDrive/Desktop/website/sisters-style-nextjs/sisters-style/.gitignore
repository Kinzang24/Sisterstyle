# dependencies
/node_modules

# next.js
/.next/
/out/

# production
/build

# database (local dev data — don't ship your real store's data in git)
/data/*.db
/data/*.db-journal
/data/*.db-wal
/data/*.db-shm

# uploaded images
/public/uploads/*
!/public/uploads/.gitkeep

# env
.env
.env.local
.env*.local

# misc
.DS_Store
*.pem
npm-debug.log*
